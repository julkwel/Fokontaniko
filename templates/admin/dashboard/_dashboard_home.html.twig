{% extends 'admin/_index_admin.html.twig' %}
{% set menu_dashboard = true %}
{% block content %}
    <div class="row">
        <!-- seo fact area start -->
        <div class="col-lg-12">
            <div class="row">
                <div class="col-md-6 mt-5 mb-3">
                    <a href="{{ path('mponina_list') }}" class="card">
                        <div class="seo-fact sbg1">
                            <div class="p-4 d-flex justify-content-between align-items-center">
                                <div class="seofct-icon">
                                    <i class="fa fa-group"></i>
                                    Isan'ny mponina
                                </div>
                                <h2>{{ mponina }}</h2>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="col-md-6 mt-md-5 mb-3">
                    <a href="{{ path('list_employee') }}" class="card">
                        <div class="seo-fact sbg2">
                            <div class="p-4 d-flex justify-content-between align-items-center">
                                <div class="seofct-icon">
                                    <i class="fa fa-user-secret"></i>
                                    Isan'ny mpiasa
                                </div>
                                <h2>{{ employes }}</h2>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </div>
    <div class="card">
        <div class="card-header">
            <h1 class="text-center">Salan'isan'ny mponina</h1>
        </div>
        <div class="card-body">
            <div class="chart">
                <canvas id="myChart" width="200" height="100"></canvas>
            </div>
        </div>
    </div>
{% endblock %}
